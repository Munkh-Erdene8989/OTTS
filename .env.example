# Copy values into the app-specific files below.
#   cp apps/api/.env.example apps/api/.env
#   cp apps/web/.env.example apps/web/.env

# API — see apps/api/.env.example
# WEB — see apps/web/.env.example
